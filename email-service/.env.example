# ===========================================
# Email Service Environment Configuration
# ===========================================

# Server Port
PORT=3000

# ===========================================
# SMTP Configuration (Mailtrap Production SMTP)
# Get these from: Mailtrap Dashboard > Sending Domains > ghagevaibhav.in > Integration
# ===========================================

# SMTP Server Host (Mailtrap's transactional email server)
SMTP_HOST=live.smtp.mailtrap.io

# SMTP Port (587 for TLS, 465 for SSL, 25 for unencrypted)
SMTP_PORT=587

# Set to 'true' if using port 465 (SSL)
SMTP_SECURE=false

# Mailtrap API Token (get from Mailtrap Dashboard > API Tokens)
# Note: For SMTP, use the same token for both USER and PASS
SMTP_USER=your_mailtrap_api_token_here
SMTP_PASS=your_mailtrap_api_token_here

# ===========================================
# Sender Configuration
# Must use an email from your verified domain (ghagevaibhav.in)
# ===========================================
SMTP_FROM_EMAIL=noreply@ghagevaibhav.xyz
SMTP_FROM_NAME=OCRS System

# ===========================================
# Optional: Connection Pool Settings
# ===========================================
SMTP_MAX_CONNECTIONS=5
SMTP_MAX_MESSAGES=100

# ===========================================
# Optional: TLS Settings
# ===========================================
# Set to 'false' only for debugging certificate issues
SMTP_TLS_REJECT_UNAUTHORIZED=true
